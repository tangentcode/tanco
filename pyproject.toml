[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"


[project]
name = "rogo"
version = "0.1.0"
description='A tool for test-driven teaching.'

[project.scripts]
rogo = "rogo.driver:main"

[project.license]
file = "LICENSE"